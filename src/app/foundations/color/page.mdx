export const metadata = {
  title: "Color — Foundations",
};

## Color

We’ll define the Everyday palette as a set of tokens (Navy / Green / Khaki / Neutrals) with ramps (50–950).

### Status

The exact hex values aren’t wired yet. Once you provide the token hexes, we’ll:

- encode them into Tailwind theme tokens
- generate a swatch grid
- add contrast/accessibility notes


