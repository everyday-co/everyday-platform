## Everyday Design System — Playbook

This repo scaffolds a public Playbook site for **Everyday Design System**, initially mirroring the structure and page patterns of the eBay Playbook (structure-first, content placeholders).

### Local development
1) Install dependencies

```bash
npm install
```

2) Run dev server

```bash
npm run dev
```

### Brand assets
Current logo assets are in `logo_files/`. For a production Next.js deployment, assets should be served from `public/`.


